package schema

import (
	"entgo.io/ent"
	"entgo.io/ent/schema/field"
)

// {{.EntTypeName}} holds the schema definition for the {{.EntTypeName}} entity.
type {{.EntTypeName}} struct {
	ent.Schema
}

// Fields of the {{.EntTypeName}}.
func ({{.EntTypeName}}) Fields() []ent.Field {
	return []ent.Field{
		field.String("name").
			NotEmpty().
			Comment("Nom de {{.Label | lower}}"),
		
		// TODO: Ajouter d'autres champs ici
		// Exemples:
		// field.String("email").Optional(),
		// field.Time("created_at").Default(time.Now),
		// field.Bool("is_active").Default(true),
		// field.Int("quantity").Default(0),
	}
}

// Edges of the {{.EntTypeName}}.
func ({{.EntTypeName}}) Edges() []ent.Edge {
	return nil
	
	// TODO: Définir les relations ici
	// Exemples:
	// edge.To("posts", Post.Type),
	// edge.From("category", Category.Type).Ref("products").Unique(),
}

// Indexes of the {{.EntTypeName}}.
func ({{.EntTypeName}}) Indexes() []ent.Index {
	return nil
	
	// TODO: Ajouter des index ici si nécessaire
	// Exemples:
	// index.Fields("email").Unique(),
	// index.Fields("name", "created_at"),
}

// Hooks of the {{.EntTypeName}}.
func ({{.EntTypeName}}) Hooks() []ent.Hook {
	return nil
	
	// TODO: Ajouter des hooks ici si nécessaire
	// Pour validation custom, auto-population, etc.
}

// Annotations of the {{.EntTypeName}}.
func ({{.EntTypeName}}) Annotations() []ent.Annotation {
	return nil
	
	// TODO: Ajouter des annotations ici si nécessaire
	// Pour GraphQL, OpenAPI, etc.
}
